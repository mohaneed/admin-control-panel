{% extends "layouts/base.twig" %}

{% block title %}Activity Logs | {{ ui.appName }}{% endblock %}

{% block content %}
    <div class="container mx-auto px-4">

        <div class="flex items-center justify-between mb-4">
            <h1 class="text-xl font-semibold text-gray-800">
                Activity Logs
            </h1>
        </div>

        {# ============================
       Filters Bar (SAME STRUCTURE)
       ============================ #}
        <div class="bg-white border border-gray-200 rounded-lg p-4 mb-4">
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">

                <input
                        type="text"
                        id="filter-actor-id"
                        placeholder="Actor ID"
                        class="w-full border-gray-300 rounded-md shadow-sm text-sm"
                />

                <input
                        type="text"
                        id="filter-action"
                        placeholder="Action"
                        class="w-full border-gray-300 rounded-md shadow-sm text-sm"
                />

                <input
                        type="text"
                        id="filter-ip-address"
                        placeholder="IP Address"
                        class="w-full border-gray-300 rounded-md shadow-sm text-sm"
                />

                <input
                        type="date"
                        id="filter-date-from"
                        class="w-full border-gray-300 rounded-md shadow-sm text-sm"
                />

                <input
                        type="date"
                        id="filter-date-to"
                        class="w-full border-gray-300 rounded-md shadow-sm text-sm"
                />
            </div>

            <div class="mt-4 flex gap-2">
                <button
                        id="btn-search"
                        class="px-4 py-1 text-sm bg-gray-800 text-white rounded-md hover:bg-gray-700"
                >
                    Search
                </button>

                <button
                        id="btn-reset"
                        class="px-4 py-1 text-sm bg-white text-gray-600 border border-gray-600 rounded-md hover:bg-gray-600 hover:text-white"
                >
                    Reset
                </button>
            </div>
        </div>


        {# ============================
       Table Container
       ============================ #}
        <div class="container mt-6"> <div id="table-container" class="w-full"></div>
        </div>

    </div>

    <div id="metadata-modal" class="fixed inset-0 bg-black/40 hidden items-center justify-center z-50">
        <div class="bg-white rounded-lg shadow-xl w-full max-w-lg">
            <div class="px-4 py-3 border-b flex justify-between items-center">
                <h3 class="text-sm font-semibold text-gray-700">Metadata</h3>
                <button id="metadata-close" class="text-gray-500 hover:text-black">✕</button>
            </div>

            <div class="p-4 max-h-[400px] overflow-auto">
                <table class="w-full text-sm border">
                    <thead>
                    <tr class="bg-gray-50">
                        <th class="border px-3 py-2 text-left">Key</th>
                        <th class="border px-3 py-2 text-left">Value</th>
                    </tr>
                    </thead>
                    <tbody id="metadata-table-body"></tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}

{% block scripts %}
    <script src="{{ asset('assets/js/callback_handler.js') }}"></script>
    <script src="{{ asset('assets/js/useInputChecker.js') }}"></script>
    <script src="{{ asset('assets/js/data_table.js') }}"></script>
    <script src="{{ asset('assets/js/pages/activity_logs.js') }}"></script>
{% endblock %}
